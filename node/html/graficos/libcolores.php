<?

	/* Paleta de colores */
	$colores = array();
	$colores[0] = ImageColorAllocate($image,0,0,0);        // Negro
	$colores[1] = ImageColorAllocate($image,100,100,100);  // Gris oscuro
	$colores[2] = ImageColorAllocate($image,160,160,160);  // Gris medio
	$colores[3] = ImageColorAllocate($image,220,220,220);  // Gris claro
	$colores[4] = ImageColorAllocate($image,255,255,255);  // blanco
	
	$colores[5] = ImageColorAllocate($image,36,171,232);      // Azul dos
	$colores[6] = ImageColorAllocate($image,25,139,190);      // Azul dos obscuro
	$colores[7] = ImageColorAllocate($image,127,200,233);  // Azul dos claro

	$colores[8] = ImageColorAllocate($image,47,208,51);      // Verde
	$colores[9] = ImageColorAllocate($image,0,180,0);      // Verde obscuro
	$colores[10] = ImageColorAllocate($image,150,255,150);  // Verde claro

	$colores[11] = ImageColorAllocate($image,255,0,0);      // Rojo
	$colores[12] = ImageColorAllocate($image,180,0,0);      // Rojo obscuro
	$colores[13] = ImageColorAllocate($image,255,150,150);  // Rojo claro
	
	$colores[14] = ImageColorAllocate($image,147,147,147);      // Gris
	$colores[15] = ImageColorAllocate($image,131,131,131);      // Gris obscuro
	$colores[16] = ImageColorAllocate($image,204,204,204);  // Gris claro
	
	$colores[17] = ImageColorAllocate($image,234,33,222);      // Rosa
	$colores[18] = ImageColorAllocate($image,181,31,172);      // Rosa obscuro
	$colores[19] = ImageColorAllocate($image,242,125,235);  // Rosa claro

	$colores[20] = ImageColorAllocate($image,255,168,32);      // Naranja
	$colores[21] = ImageColorAllocate($image,226,141,7);      // Naranja obscuro
	$colores[22] = ImageColorAllocate($image,247,186,91);  // Naranja claro

	$colores[23] = ImageColorAllocate($image,166,61,255);      // Morado
	$colores[24] = ImageColorAllocate($image,130,22,221);      // Morado obscuro
	$colores[25] = ImageColorAllocate($image,187,108,253);  // Morado claro

	$colores[26] = ImageColorAllocate($image,0,0,255);      // Azul
	$colores[27] = ImageColorAllocate($image,0,0,180);      // Azul obscuro
	$colores[28] = ImageColorAllocate($image,150,150,255);  // Azul claro

	$colores[29] = ImageColorAllocate($image,211,226,44);      // Amarillo
	$colores[30] = ImageColorAllocate($image,176,189,31);      // Amarillo obscuro
	$colores[31] = ImageColorAllocate($image,233,244,115);  // Amarillo claro

	$colores[32] = ImageColorAllocate($image,227,130,45);      // Naranja dos
	$colores[33] = ImageColorAllocate($image,193,112,39);      // Naranja dos obscuro
	$colores[34] = ImageColorAllocate($image,211,154,104);  // Naranja dos claro

?>